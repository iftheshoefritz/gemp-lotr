<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.gempukku.lotro</groupId>
    <artifactId>gemp-lotr</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>gemp-lotr</name>
    <url>http://maven.apache.org</url>

    <modules>
        <module>gemp-lotr-server</module>
        <module>gemp-lotr-logic</module>
        <module>gemp-lotr-common</module>
        <module>gemp-lotr-cards</module>
        <module>gemp-lotr-async</module>
        <module>gemp-lotr-images</module>
    </modules>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <java.version>21</java.version>
    </properties>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.8.1</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <encoding>UTF-8</encoding>
                </configuration>
            </plugin>

            <!-- Utility commands for cleaning bot training artifacts -->
            <plugin>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>3.1.0</version>
                <inherited>false</inherited>
                <executions>
                    <execution>
                        <id>clean-models</id>
                        <!-- No phase binding - only runs when explicitly invoked -->
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <delete>
                                    <fileset dir="bot-models" includes="**/*.model"/>
                                </delete>
                                <echo message="Removed all .model files from bot-models/"/>
                            </target>
                        </configuration>
                    </execution>

                    <execution>
                        <id>clean-training-data</id>
                        <!-- No phase binding - only runs when explicitly invoked -->
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <delete>
                                    <fileset dir="." includes="*.jsonl"/>
                                </delete>
                                <echo message="Removed all .jsonl training data files"/>
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
    <repositories>
        <repository>
            <id>maven2-repository.java.net</id>
            <name>Java.net Repository for Maven</name>
            <url>http://download.java.net/maven/2/</url>
            <layout>default</layout>
        </repository>

    </repositories>
</project>
